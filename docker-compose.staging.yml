services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.staging
    container_name: taskmanager-api-staging
    ports:
      - "28000:8000"
    env_file:
      - .env.staging
    environment:
      - WORKERS=1
    restart: unless-stopped
