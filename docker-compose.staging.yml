services:
  api:
    build: .
    container_name: taskmanager-api-staging
    ports:
      - "28000:8000"
    env_file:
      - .env
    environment:
      - WORKERS=1
    volumes:
      - uploads-staging:/app/uploads
    restart: unless-stopped

volumes:
  uploads-staging:
