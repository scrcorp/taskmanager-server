# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.30.0
gunicorn==23.0.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.0

# Auth
PyJWT==2.9.0
bcrypt==5.0.0
python-jose[cryptography]==3.3.0
user-agents==2.2.0

# Validation & Config
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1

# HTTP Client
httpx==0.27.0

# Excel
openpyxl==3.1.5

# Logging
axiom-py==0.10.0

# Email
aiosmtplib==3.0.2

# AWS
boto3==1.35.0
fpdf2==2.8.7

# PDF document rendering (HTML/CSS → PDF for warning documents)
# NOTE: WeasyPrint needs native libs on the host — pango, cairo, gdk-pixbuf
#       (macOS: `brew install pango gdk-pixbuf`; Debian/EC2: libpango-1.0-0
#        libpangocairo-1.0-0 libgdk-pixbuf-2.0-0 libffi-dev). See deploy notes.
weasyprint==69.0
jinja2==3.1.6

# Scheduler (cron jobs for attendance state transitions)
APScheduler==3.10.4

# Image pipeline — WebP 인코딩/썸네일 (storage_service). 이전엔 weasyprint 의
# transitive 의존이었으나 직접 사용하므로 명시 고정.
Pillow==12.2.0
# HEIC/HEIF 디코드 — 아이폰 사진(HEIC)을 WebP 로 변환하기 위한 Pillow 플러그인.
# manylinux/macOS wheel 에 libheif 번들 포함 → prod EC2 에 시스템 패키지 불필요.
pillow-heif==0.21.0
