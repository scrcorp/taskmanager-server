services:
  api:
    build: .
    ports:
      - "18000:8000"
    env_file:
      - .env
    environment:
      - WORKERS=1
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
